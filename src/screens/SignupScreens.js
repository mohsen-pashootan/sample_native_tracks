import React from "react";
import { View, StyleSheet, Text } from "react-native";

const SignupScreens = () => {
  return <Text style={{ fontSize: 48 }}>SignupScreens</Text>;
};

const styles = StyleSheet.create({});

export default SignupScreens;
